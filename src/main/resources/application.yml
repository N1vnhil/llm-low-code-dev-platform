spring:
  application:
    name: llm-low-code-platform
server:
  port: 8080
  servlet:
    context-path: /api
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: org.n1vnhil.llm.lowcode.dev.platform.controller

# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn
